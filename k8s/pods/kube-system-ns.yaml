apiVersion: v1
kind: Pod
metadata:
  name: root-pod
  namespace: kube-system
spec:
  containers:
  - name: my-container
    image: busybox
    resources:
      limits:
        cpu: 100m
        memory: 256Mi
    command: ["sh", "-c", "echo 'Hello, Kubernetes!' >> /var/hello.txt"]
